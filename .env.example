# Supabase project credentials (https://supabase.com/dashboard)
# Create a free project, then copy these from Project Settings -> API.
# Leave both empty to build WITHOUT the sign-in gate (handy for local dev).
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
